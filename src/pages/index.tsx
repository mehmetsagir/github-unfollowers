import type { NextPage } from 'next';

const Home: NextPage = () => {

  return (
    <div>
      {/* <button onClick={() => logout({ returnTo: process.env.NEXT_PUBLIC_URL })}>
        Log Out
      </button> */}
    </div>
  );
};

export default Home;
